"""Component 1 — the time-gated data API.

The host-side feeder, the framed pipe protocol between host and cell, and the
strategy-side `Market` client that is the only object a strategy ever holds.
Milestone 4.
"""
